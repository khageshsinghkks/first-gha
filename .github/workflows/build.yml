name: test-first-gha
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: print greeting
        run: echo "hello"
      - name: print bye
        run: echo "bye"
